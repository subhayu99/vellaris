dist
node_modules
coverage
pnpm-lock.yaml
tests/fixtures
public
# openapi.json is regenerated by `make gen-openapi` and a CI guard verifies
# the committed copy is byte-identical to the script's output. Prettier's
# JSON formatter disagrees with Python's `json.dumps(indent=2)` on
# array-element wrapping, so leaving it under prettier's purview means
# the two formatters fight every release.
src/docs/pages/api/openapi.json
